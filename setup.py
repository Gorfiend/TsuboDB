#!/usr/bin/env python

import os
from distutils.core import setup, Extension

setup(
    name = 'tsubodb',
    version = '0.9.0',
    author = 'Gorfiend',
    url = 'TODO',
    packages = ['tsubodb'],
    scripts = ['tsubodb'])
